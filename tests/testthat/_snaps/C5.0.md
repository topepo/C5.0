# C5.0 errors on non-factor outcome

    Code
      C5.0(dat[, -1], y = as.character(dat$y))
    Condition
      Error in `C5.0()`:
      ! `y` must be a factor, not a character vector.

# C5.0 errors on missing column names

    Code
      C5.0(x, dat$y)
    Condition
      Error in `C5.0()`:
      ! `x` must have column names.

# C5.0 errors on invalid cost matrix (not a matrix)

    Code
      C5.0(dat[, -1], dat$y, costs = c(1, 2, 3, 4))
    Condition
      Error in `C5.0()`:
      ! `costs` must be a matrix, not a double vector.

# C5.0 errors on cost matrix dimension mismatch

    Code
      C5.0(dat[, -1], dat$y, costs = bad_costs)
    Condition
      Error in `C5.0()`:
      ! `costs` must be a 2x2 matrix.
      x Provided: 3x3.

# C5.0 errors on invalid trials value (too low)

    Code
      C5.0(dat[, -1], dat$y, trials = 0)
    Condition
      Error in `C5.0()`:
      ! `trials` must be between 1 and 100, not 0.

# C5.0 errors on invalid trials value (too high)

    Code
      C5.0(dat[, -1], dat$y, trials = 101)
    Condition
      Error in `C5.0()`:
      ! `trials` must be between 1 and 100, not 101.

# C5.0 errors on invalid x type

    Code
      C5.0(as.list(dat[, -1]), dat$y)
    Condition
      Error in `C5.0()`:
      ! `x` must have column names.

# C5.0 errors on non-numeric weights

    Code
      C5.0(dat[, -1], dat$y, weights = rep("a", 100))
    Condition
      Error in `C5.0()`:
      ! `weights` must be numeric, not a character vector.

# C5.0Control errors on invalid CF

    Code
      C5.0Control(CF = 1.5)
    Condition
      Error in `C5.0Control()`:
      ! `CF` must be between 0 and 1, not 1.5.

# C5.0Control errors on invalid sample (too high)

    Code
      C5.0Control(sample = 1)
    Condition
      Error in `C5.0Control()`:
      ! `sample` must be between 0 and 0.999, not 1.

# C5.0Control errors on invalid bands

    Code
      C5.0Control(bands = 1)
    Condition
      Error in `C5.0Control()`:
      ! `bands` must be between 2 and 10000, not 1.

# C5.0 errors when cost matrix missing row/col names

    Code
      C5.0(dat[, -1], dat$y, costs = costs)
    Condition
      Error in `C5.0()`:
      ! `costs` must have both row and column names.

# C5.0 warns about rule banding without rules

    Rule banding only works with rules.
    i `rules` was changed to "TRUE".

# C5.0 warns about cost matrix without dimnames

    No dimnames were given for the cost matrix.
    i The factor levels will be used.

# print.C5.0 works for tree model

    Code
      print(mod)
    Output
      
      Call:
      C5.0.default(x = dat[, -1], y = dat$y)
      
      Classification Tree
      Number of samples: 100 
      Number of predictors: 3 
      
      Tree size: 4 
      
      Non-standard options: attempt to group attributes
      

# print.C5.0 works for rules model

    Code
      print(mod)
    Output
      
      Call:
      C5.0.default(x = dat[, -1], y = dat$y, rules = TRUE)
      
      Rule-Based Model
      Number of samples: 100 
      Number of predictors: 3 
      
      Number of Rules: 0 
      
      Non-standard options: attempt to group attributes
      

# print.C5.0 works for boosted model

    Code
      print(mod)
    Output
      
      Call:
      C5.0.default(x = dat[, -1], y = dat$y, trials = 5)
      
      Classification Tree
      Number of samples: 150 
      Number of predictors: 3 
      
      Number of boosting iterations: 5 requested;  1 used due to early stopping
      
      Non-standard options: attempt to group attributes
      

# failed model

    Code
      C5.0(Species ~ zv, data = iris_zv)
    Output
      c50 code called exit with value 1
    Condition
      Error in `C5.0.default()`:
      ! `C5.0()` failed with error:
      x line 6 of `undefined.names': attribute `zv' has only one value `a'

